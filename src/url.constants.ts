export namespace UrlConstants {
    export const REGEX_BRACE_PARAMS = /({)(.*?)(})/g;
    export const REGEX_FILENAME = /(.+)(\.[a-zA-Z0-9]+)$/;
    export const URL_PATH_PREFIX = ':';
    export const URL_PATH_SEPARATOR = '/';
    export const URL_EXT_SEPARATOR = '.';
}
